preload = ~/Pictures/wal.png
# .. more preloads

#set the default wallpaper(s) seen on initial workspace(s) --depending on the number of monitors used
wallpaper = ,~/Pictures/wal.png

splash = false
#fully disable ipc
# ipc = off

